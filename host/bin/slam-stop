#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/../../scripts/env.sh"
# Mode-aware stop preserves mapping output before removing our container.
"$G1_SLAM_PROJECT_DIR/host/bin/g1-motion-stop"
exec "$G1_SLAM_PROJECT_DIR/scripts/stop_container.sh"

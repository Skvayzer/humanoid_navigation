#!/usr/bin/env bash
# The old script used rsync --delete against a fixed robot path.
echo 'Clone this repository into a NEW directory on the target robot.' >&2
echo 'Follow docs/INSTALL.md, then run scripts/install_shortcuts.sh locally.' >&2
echo 'No files were transferred or changed.' >&2
exit 2
